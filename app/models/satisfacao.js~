var mongoose	= require('mongoose');
var Schema	= mongoose.Schema;

var SatisfacaoSchema = new Schema({
   curtir: Number,
   data: String
});

module.exports = mongoose.model('Satisfacao', SatisfacaoSchema);
